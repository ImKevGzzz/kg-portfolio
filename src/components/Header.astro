---
import PageTheme from "./PageTheme";
---

<header class="flex px-14 justify-between dark:text-white text-darkModeColor w-full py-5 items-center">
    <section class="flex items-center gap-3">
        <p class="font-semibold text-base">Es</p>
        <label class="relative inline-flex  cursor-pointer items-center">
            <input id="switch" type="checkbox" class="peer sr-only" />
            <label for="switch" class="hidden"></label>
            <div class="peer h-6 w-11 rounded-full bg-slate-200 after:absolute after:left-[2px] after:top-0.5 after:h-5 after:w-5 after:rounded-full after:bg-darkModeColor after:transition-all after:content-[''] peer-checked:after:translate-x-full"></div>
        </label>
        <p class="font-semibold text-base">En</p>
    </section>
    <nav class="gap-5 flex">
        <a>Sobre mí</a>
        <a>Experiencia</a>
        <a>Proyectos</a>
        <a>Habilidades y Herramientas</a>
        <a>Contacto</a>
    </nav>
    <section>
        <PageTheme client:visible/>
    </section>
</header>

<style>
    nav > a {
        cursor: pointer;
        font-size: 16px;
        font-weight: 600;
        padding-inline: 10px;
        transition: ease .4s color;
    }

    nav > a:hover {
        color: #FF9040;
    }
</style>